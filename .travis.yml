language: python
sudo: false

python:
  - "2.7"
  - "3.5"

install:
  - pip install -r requirements.txt

script:
  - export PYTHONPATH=$PYTHONPATH:.
  - py.test --cov gitstats --durations=10

after_success:
- coveralls
